export function setupEnrollment() { const modal = document.querySelector("[data-modal]"); const open = plan => { modal.classList.add("open"); modal.querySelector("select").value = plan || "single"; document.body.classList.add("locked"); modal.querySelector("input")?.focus(); }; const close = () => { modal.classList.remove("open"); document.body.classList.remove("locked"); }; document.addEventListener("click", event => { if (event.target.closest("[data-enroll]")) open(event.target.closest("[data-enroll]").dataset.plan); if (event.target.closest("[data-close]") || event.target === modal) close(); }); document.addEventListener("keydown", event => { if (event.key === "Escape") close(); }); document.querySelector("[data-enrollment-form]").addEventListener("submit", event => { event.preventDefault(); const form = event.currentTarget; const values = Object.fromEntries(new FormData(form)); const error = form.querySelector("[data-form-error]"); const digits = values.whatsapp.replace(/\D/g, ""); if (values.name.trim().length < 2 || values.school.trim().length < 2 || digits.length < 7 || digits.length > 15) { error.textContent = "Kuch details adhoori hain. Naam, school aur valid WhatsApp number check kariye."; return; } const entries = JSON.parse(localStorage.getItem("cohortify_enrollments") || "[]"); entries.push({ ...values, submittedAt: new Date().toISOString() }); localStorage.setItem("cohortify_enrollments", JSON.stringify(entries)); form.reset(); error.textContent = ""; modal.querySelector("[data-form-view]").hidden = true; modal.querySelector("[data-success]").classList.add("show"); }); document.querySelector("[data-success] [data-close]").addEventListener("click", () => { modal.querySelector("[data-form-view]").hidden = false; modal.querySelector("[data-success]").classList.remove("show"); close(); }); }